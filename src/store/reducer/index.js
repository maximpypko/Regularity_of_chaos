import {

} from "../actions/types";

const initialState = {
  coordinates: {
    A: [],
    B: [],
    C:[],
  }
}

const reducer = (state = initialState, { type, payload }) => {
  return state;
}

export default reducer;